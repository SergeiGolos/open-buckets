# Open Buckets Configuration File
# Gitignore-style patterns for inclusion/exclusion

# === Inclusion Patterns ===
# Lines starting without a prefix are included in context
src/**/*.js
src/**/*.ts
docs/**/*.md
README.md
package.json
tsconfig.json
.env.example

# === Exclusion Patterns ===
# Lines starting with ! are excluded from context
!node_modules/**
!dist/**
!build/**
!.git/**
!*.test.js
!*.spec.ts
!*.log
!*.tmp

# === Directory Grep ===
# [dirs:path] sections define grep patterns for specific directories
# When a file is dropped, these patterns are searched in the directories

[dirs:./configs]
error
debug
warning
deprecated
TODO

[dirs:./templates]
TODO
FIXME
HACK
XXX
BUG

# Notes:
# 1. Patterns are relative to working directory where open-buckets is run
# 2. Use ** for recursive directory matching (e.g., src/**/*.js)
# 3. Use ! prefix to exclude (e.g., !node_modules/**)
# 4. File-type specific rules: create {ext}.bucket-include files
#    (e.g., .js.bucket-include for JavaScript files)
# 5. Global config: ~/.config/open-buckets/.bucket-include
